export interface IGoogleApiConfig {
    // The account that will be emulated to request the API information
    authenticationSubject: string,
    googleAppOrganisationId: string,
    base64EncodedKeyFile: string
}
